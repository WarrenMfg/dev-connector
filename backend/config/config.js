import { URI_dev, secret_dev, expiresIn_dev, githubAuthToken_dev } from './config_dev';

export const URI = URI_dev;

export const secret = secret_dev;

export const expiresIn = expiresIn_dev;

export const githubAuthToken = githubAuthToken_dev;

